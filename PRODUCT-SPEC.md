# VideoSize Composer 2026v4-33 验收规格

状态：功能与视觉收口，等待便携包和发布流水线验证。当前版本：2026v4-33。
目标：提供接近 Adobe Media Encoder 的清晰批量编码流程，并把源视频的创建日期和修改时间原样恢复到输出文件。

## 版本号规则

- 技术版本字段使用合法 SemVer `年.功能升级.修复错误`，例如 `2026.0.1`。
- 对用户展示的版本格式为 `年v功能升级-修复错误`，例如 `2026v0-1`。
- 主标题只显示 `年v功能升级`，设置界面和发布元数据显示完整展示版本。
- 当前版本为 `2026v4-33`（技术版本 `2026.4.33`）。

## 必须达成的用户结果

1. 可通过文件、文件夹或原生拖放批量导入视频与音频，并可从文件夹或任意序列帧识别图像序列。
2. 可保存和复用“我的预设”；预设抽屉可折叠，预设在独立窗口中创建、编辑和删除，与右侧当前任务输出设置相互独立。
3. 色深与色度采样默认支持“原视频参数”，批量任务会按每个源文件分别解析并继承像素格式。
4. 支持 H.264、H.265、AV1、ProRes 422 LT；支持源分辨率、短边分辨率、10%–90% 倍率和自定义宽高；自动硬件选择只使用当前平台实际可用的编码器，失败时可回退 CPU。
5. 输出目录在启动时检查可创建、可写和剩余空间；重名自动编号。
6. 编码先写同目录临时文件，成功后再移动到最终文件；失败或取消清除临时文件。
7. 默认恢复并回读验证源文件的创建日期与修改时间；任一时间恢复失败必须显示明确失败状态，不能显示为成功。
8. 任务可取消；成功、失败和取消是互斥终态，运行中事件不得提前结束会话。
9. 1440×1024 与最小 1180×760 窗口下主流程、队列、设置和主动作均可访问，无页面级横向溢出。
10. 支持全部输出到指定文件夹、原位输出、原位子文件夹输出；支持原名或前后缀命名。
11. 支持批量套用 LUT，LUT 文件和强度随预设保存。
12. 可识别带全景元数据的源视频和 2:1 全景候选；全景导出写入 V1 UUID 与 V2 `st3d`/`sv3d` 标准元数据并回读验证。
13. 序列帧媒体可独立设置帧率、分辨率和像素宽幅放大率；默认帧率可在应用设置中修改；透明 PNG/TGA 序列与 ProRes 4444 Alpha 可单独导出黑白 Alpha 遮罩，遮罩码率约为主视频的 30%。
14. 支持 WAV/MP3 等音频导入、音频转视频的时间码/纯黑/纯白画面模式，以及为无音轨视频或序列帧附加外接音频；输出封装默认保持源后缀（序列与音频默认 MP4），并可显式选择常见视频与音频容器。
15. 预设、应用偏好和导入的 LUT 默认保存在运行程序旁的 `config/`；首次升级只在便携目录不存在时迁移旧版 Tauri 配置，不删除旧数据，LUT 子目录结构与相对引用保持可用。
16. 输出设置页可统一输出目标帧率：默认“保持原帧率”，也可选择常见帧率（23.976/24/25/29.97/30/50/59.94/60）或输入自定义值；输出通过 FFmpeg `-r` 重新定时，杜比视界保留导出保持源帧率。

## 专业视频能力与安全边界

- SDR、HLG、HDR10 的转换必须通过 `zscale` 实际转换像素并写入匹配的色彩元数据，不能只修改标签。
- HLG/HDR10 仅允许 H.265、AV1 或 ProRes 的 10-bit 输出，并使用 CPU 路径保证组合可用。
- 4:2:2 支持 H.265、AV1 和 ProRes；H.264 10-bit 或 4:2:2 不作为承诺组合。
- Dolby Vision 支持“保留导出”：对已识别的 Dolby Vision HEVC 源执行无损视频流复制以保留 RPU；不允许缩放和 LUT，也不生成新的 Dolby Vision。
- LUT、缩放、位深、HDR 和全景元数据路径必须保留真实编码测试；FFmpeg 与 ffprobe 必须同时可用。
- 未经设备实测校准时不显示伪精确耗时，界面显示“待首次校准”。

## 发布证据

- 前端 TypeScript/Vite 构建通过。
- Rust 测试覆盖真实 H.264/H.265/AV1/ProRes、HLG/HDR10、10-bit/4:2:2、LUT、全景 V1/V2 元数据、时间恢复、导入、重名、临时文件和取消会话；新增透明 PNG Alpha 遮罩（无音轨、尺寸与信号）、ProRes 4444 Alpha 路径、WAV/音频转视频（三种画面模式）、静音视频附加短外接音频并保持视频时长的真实媒体测试。
- 便携配置测试覆盖首次迁移与嵌套 LUT、已有便携配置优先、偏好回退持久化、同根相对 LUT 与跨盘绝对路径；编码路径会解析相对 LUT 并通过 `lut3d` 使用实际文件。
- Windows 原生端到端证据必须包含 UI 终态、ffprobe 输出、创建日期和修改时间的磁盘比对。
- Windows 便携包可构建；macOS `.app` 由 macOS CI 构建并执行同一 Rust/FFmpeg 测试矩阵。
- 选定视觉方向具有同尺寸对比、完成态和最小窗口截图证据。

## 2026v4-33 不承诺的能力

- 从普通 HDR/SDR 素材生成新的 Dolby Vision RPU，或对 Dolby Vision 做缩放、调色、LUT 后仍声称保留动态元数据。
- 应用重启后续跑未完成编码。
- 未经校准的精确剩余时间预测。

这些边界是为了避免表面存在、实际不正确的专业视频能力；后续版本应在独立样片与平台矩阵完整验证后再开放。
